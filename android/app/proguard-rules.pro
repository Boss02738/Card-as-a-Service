# ลบชื่อไฟล์ Source code จริงออกเพื่อความปลอดภัย (พิมพ์ให้ถูกตามนี้ครับ)
-renamesourcefileattribute SourceFile
-keepattributes SourceFile,LineNumberTable

# ตัวเลือกสำหรับการ Optimize เพิ่มเติม (เลือกใช้ตัวใดตัวหนึ่งหรือทั้งหมด)
-optimizations !code/allocation/variable